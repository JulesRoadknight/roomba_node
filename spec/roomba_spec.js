describe("roomba", function() {
  var Roomba = require('../lib/roomba');

  beforeEach(function(){
    roomba = new Roomba();
  })

  // it("template of throw error", function() {
  //   expect(() => {roomba.test()}).toThrow()
  // });

  // it("template of equal", function() {
  //   expect(roomba.test).toEqual('Running the roomba test');
  // });

  // it("template of toBe", function() {
  //   expect(roomba.test).toBe(true)
  // });

});
